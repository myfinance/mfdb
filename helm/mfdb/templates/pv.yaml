apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.stage }}_{{ .Values.pv.mfdbpvc }}
spec:
  storageClassName: {{ .Values.stage }}_mfdbstorage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pv.mfdbstoragesize }}   

---

apiVersion: v1
kind: PersistentVolume
metadata:
  name: {{ .Values.stage }}_localstorage
spec:
  storageClassName: {{ .Values.stage }}_mfdbstorage
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: {{ .Values.pv.mfdbstoragesize }}    
  hostPath:
    path: {{ .Values.pv.mfdbstorage }}/{{ .Values.stage }}db
    type: Directory  

